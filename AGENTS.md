# AGENTS.md -- 归拢：给编码 agent 的安装与引导手册

你要帮用户装上 **归拢**。你的任务不只是把它跑起来 —— 还要让用户明白自己拿到了什么。

---

## Step 0 -- 先讲清楚这是什么

动手之前，告诉用户他即将得到什么：

> **归拢**接管你的新标签页，把你此刻开着的所有标签页按域名摆成一张仪表盘。
>
> 它好在哪：
> - **一眼看尽** 打开的标签页按域名分组，网格排开
> - **主页单独成组** Gmail、X、LinkedIn、YouTube、GitHub 主页收进一张卡片，一次关干净
> - **关得爽** 关闭时有合成音效 + 彩带
> - **重复检测** 同一个页面开了两遍会被标出来
> - **点标题就跳** 直接跳到那一页，跨窗口也行
> - **存入稍后再看** 关之前先把想留的存进清单
> - **当地天气** 页头下面一行气温和天气，城市搜着选，默认上海，可关
> - **不需要 API key** 没有服务器、不用账号
>
> 就是一个 Chrome 扩展，装起来大约 1 分钟。

---

## Step 1 -- 克隆仓库

```bash
git clone https://github.com/qwsir32-star/guilong.git
cd guilong
```

---

## Step 2 -- 装扩展

这一步必须用户自己动手。尽量让过程省事，别让他在文件选择框里翻目录。

**先** 打印 `extension/` 的绝对路径：

```bash
echo "Extension folder: $(cd extension && pwd)"
```

**再** 把路径复制进剪贴板：

- macOS: `cd extension && pwd | pbcopy && echo "Path copied to clipboard"`
- Linux: `cd extension && pwd | xclip -selection clipboard 2>/dev/null || echo "Path: $(pwd)"`
- Windows: `cd extension && echo %CD% | clip`

**再** 打开扩展管理页：

```bash
open "chrome://extensions"
```

**然后** 一步一步带着用户做：

> 路径已经复制到你的剪贴板了。接下来：
>
> 1. 你应该看到 Chrome 的扩展管理页。在**右上角**打开**开发者模式**（一个开关）。
> 2. 打开后左上角会出现**「加载已解压的扩展程序」**按钮，点它。
> 3. 会弹出文件选择框。**按 Cmd+Shift+G**（Mac）或 **Ctrl+L**（Windows / Linux）打开「前往文件夹」，**粘贴**刚才那条路径（Cmd+V / Ctrl+V）后回车。
> 4. 点**「选择」**，扩展就装上了。
>
> 装完你应该能在扩展列表里看到「归拢」。

**另外**，顺手把文件浏览器也打开到 extension 目录，作为兜底（用户可能更习惯自己拖）：

- macOS: `open extension/`
- Linux: `xdg-open extension/`
- Windows: `explorer extension\\`

---

## Step 3 -- 带用户逛一圈

装好之后：

> 成了。打开一个**新标签页**，你看到的就是归拢。
>
> 它是这么工作的：
> 1. **你开着的标签页按域名分组**，摆在网格里。
> 2. **主页**（Gmail 收件箱、X 首页、YouTube 等）单独一组，放在最前面。
> 3. **点任意标签页标题** 直接跳到那一页，跨窗口也行，不会多开一个标签页。
> 4. **点标题旁边的 ×** 只关这一个，有音效和彩带。
> 5. **点整组的「关闭全部 N 个」** 把一组关干净。第一次点会问你「确认关闭 N 个标签页？再点一次」，防手滑。
> 6. **开重了的页面**带一个琥珀色的重复标记，点「关闭 N 个重复」只留一份。
> 7. **关之前想留着**，点书签图标「存入稍后再看」，它会出现在右边的清单里。
> 8. **顶部那条常用站点**：按住就能拖动排序，点一下永远是新开一个标签页，不会去找已经开着的那一个。
> 9. **页头的搜索框**：输入网址直接打开，否则交给你自己设的默认搜索引擎（不硬编码任何一家）。
> 10. **右上角的齿轮**可以关掉「常用站点条」「搜索框」「当地天气」。关掉只是不显示 —— 钉过的站点、
>     选过的天气城市一条都不会丢。
> 11. **齿轮里还能搜天气城市**：输入城市名回车，点候选里的一个就换过去了。默认是上海。
>
> 就这样。没有服务器要跑，没有配置文件要写，不需要任何 API key，装上就能用。

---

## 关键事实

- 归拢是一个**纯 Chrome 扩展**：没有服务器、加载和日常使用都不需要 Node.js / npm，也没有构建步骤。
  （唯一的例外是**重出图标**：`node tools/make-icons.js` 需要 `@resvg/resvg-js`，平时用不到。）
- 存储用 `chrome.storage.local`，跨会话保留。
- **数据本地，但有一个例外要说清楚**：标签页数据、钉住的站点、稍后再看、设置全在本地，不上传。
  唯一一次对外请求是**取天气**（可以在设置里关掉），它只把**你选的城市坐标**发给
  `api.open-meteo.com` / `geocoding-api.open-meteo.com`，不带你任何浏览数据。
  别对用户说「100% 本地、一次网络都不发」—— 加了天气之后那句话是假的。要说就说
  「除了取天气那一次，别的都不出机器」。
- 想个性化（扩展「主页」的判定范围、自定义合并分组）就建 `extension/config.local.js`，这个文件已被 `.gitignore` 忽略。**其中规则的优先级高于内置规则**，可以覆盖内置的合并逻辑。
- 界面文案全部集中在 `extension/strings.js`，默认中文。改文案只改这一张表，不要在 `app.js` 里硬编码字符串。
- 图标母版是 `extension/icons/icon.svg`，四张 PNG 全由它导出。⚠️ **判断图标好坏要看 16px，不是 128px** ——
  细线和窄缝在那个尺寸下会整片消失（五根细签会糊成一条实心条，缝一条都留不下）。
  冒烟测试有一条守卫盯着「`manifest.json` 声明的图标都存在、且实际像素尺寸和声明一致」。
- ⚠️ **`extension/manifest.json` 里的 `key` 字段不要动、不要删。** 它固定了扩展 ID；
  删掉之后 Chrome 会改用**文件夹的绝对路径**来算 ID，用户一旦移动或重命名仓库目录，
  `chrome.storage` 里的数据（钉住的站点、稍后再看、设置开关）就会全部读不到 ——
  **静默失败，没有任何报错**。`tests/smoke.js` 有一条守卫盯着它。
- **版本号 `manifest.version` 要跟着改动一起 bump，并且它是唯一的「重载成功没有」信号。**
  `chrome://extensions` 卡片上会显示这个号；改完代码后让用户刷新扩展，卡片上的号变了
  才说明新代码真的被读进去了。**这一点很要紧**，因为 unpacked 扩展最常见的坑是
  「文件已经改了，Chrome 手里那份还是旧的」—— 图标不变、新加的点击没反应，
  而且**全程不报错**；此时看版本号是唯一不用猜的判断方式。
  另外：**移除扩展会连 `chrome.storage.local` 一起清掉**（钉住的站点、稍后再看、
  设置开关全没），所以「刷新」永远优先于「移除再重载」。
- 工具栏图标的点击由 `background.js` 里的 `chrome.action.onClicked` 接住，走的是和快捷键同一个
  `focusOrOpenDashboard()`。⚠️ **`action` 一旦有了 `default_popup`，`onClicked` 就再也不会触发**，
  而且同样不报错 —— 冒烟测试有两条守卫成对盯着这件事（「有人接点击」+「没有 popup」）。
- **快捷键（`commands`）只能读，不能写。** `commands.update()` / `commands.reset()` 是 Firefox 才有的，
  Chrome 上扩展没有任何办法设定或修改自己的快捷键。设置面板里那一行做的是「显示当前绑定 +
  跳到 `chrome://extensions/shortcuts`」，这不是妥协，是唯一存在的路子。
  两条硬规则：组合里必须有一个 `Ctrl` / `Alt`（mac 上 `Command` 也算），且按键只能是
  `A–Z 0–9 Comma Period Home End PageUp PageDown Space Insert Delete 方向键 媒体键` ——
  **反斜杠不合法**，`⌘+\` 注册不了。PART 10 里有一条白名单断言把这份列表钉住了。
  另：改 `suggested_key` 后**点刷新不一定生效**，Chrome 常常要重新加载扩展才重新分配；
  而「移除再重载」会连 `chrome.storage.local` 一起清掉 —— 所以默认值只是建议，
  要改就让用户在上面那个页面改。
- **天气是本项目唯一联网的地方**，几条约束别踩：
  - **它不需要任何 `host_permissions`，别顺手加。** Open-Meteo 的响应头带
    `access-control-allow-origin: *`（实测过），扩展页可以直接按 CORS 取。一旦在 manifest 里
    声明了 `host_permissions`，用户下次重载就会被弹一个「读取您在所有网站上的数据」级别的确认框 ——
    对一个显示气温的功能来说代价太大。PART 11 有一条断言盯着这个字段不许出现。
  - **拉不到必须安静**：不弹提示条、不写「加载失败」、不删缓存。有旧数据显示旧的，没有就保持空白。
    新标签页是最不该出现错误提示的地方。`renderWeather()` 里出现 `showToast` 就会被断言抓住。
  - **缓存优先**：先用 `weatherCache` 立刻画，缓存新鲜（20 分钟内）就一次网络都不打，过期才回源。
    新标签页是高频动作，不能让网络拖住它。
  - **换城市必须丢旧缓存**（`chrome.storage.local.remove(WEATHER_CACHE_KEY)`）。不丢的话页面会拿
    上一个城市的温度配新城市的名字 —— 看着正常、但是错的、且不报错，是最难发现的一类 bug。
  - **形状不对就当没拿到**：温度不是数字一律 `null`，宁可什么都不画，也不要画一个 `0°` 上去。
  - **候选城市必须按人口筛** —— 地理编码接口会把**全国所有同名的地方**都列回来，搜「上海」
    实测给 7 条，其中只有 1 条（上海市，2487 万人）有人口字段，另外 6 条是云南 / 浙江 / 四川的
    村子，界面上显示成「上海 · 云南 · 中国」「上海 · 四川 · 中国」，用户没法分辨（有人说这样的
    结果列表「怎么玩」）。收拾办法在 `narrowPlaces()`：按人口从大到小排；**只要有一条有人口就把
    没人口的全丢掉**；一条有人口的都没有就原样列出（那种情况没得筛）。候选副标题里显示人口，
    这是用户判断「是不是我要的那个」的唯一依据。别把这个筛选删掉当成「简化」。
    配套：`count` 要拉 `WEATHER_PLACE_LIMIT`（10）条，界面只显示 `WEATHER_PLACE_SHOW`（6）条 ——
    拉少了筛完就没得选。
  - 天气码归成 11 类（28 个 WMO 码太细），**默认城市名走文案表** `weather.defaultCity`，
    所以换语言时会变成 Shanghai 而不是卡在中文。接口返回的城市名是**数据**，不进 `strings.js`，
    但插进 innerHTML 前**必须** `escapeAttr`。
  - 图标用圆 / 矩形 / 直线拼，**不要抄弧线路径** —— 小尺寸下要能算出图元形状，而不是赌抄来的 `d`。
    改图标要用 `@resvg/resvg-js` 真渲染成 16px 看，别只看矢量。⚠️ 给 SVG 根写 `fill="none"` 会让
    没有显式 fill 的圆和矩形整片消失（云会不见），而 resvg **不报错**。
- ⚠️ **会被 JS 显示/隐藏的元素，绝不放进页头网格**（`header`）。页头是写死 `grid-column` 的三栏网格
  （PART 8 有守卫），再塞一个可隐藏的孩子，`display:none` 会让它**退出网格**、自动排布随即把后面的
  兄弟往前顶 —— 表现就是「反复开关搜索框，齿轮左右跳」。天气条因此是 `.container` 的直接孩子，
  在 `</header>` **外面**，PART 11 有一条守卫按 HTML 里的先后位置钉住这件事。
- ⚠️ 设置面板的分隔线用的是 `.setting-row ~ .setting-row`，不是 `+`。城市搜索结果
  （`#weatherResults`）夹在城市行和快捷键行之间，用 `+` 的话快捷键那行的上分隔线会**静默消失** ——
  不报错，只是变丑。
- **改完代码跑一遍 `node tests/smoke.js`**（314 处断言，有几条在循环里，实际跑 342 条；零依赖、
  一秒跑完；其中网络和图片是受控的假环境，不会真的联网）：
  它既验证内部纯函数的行为，也扫描源码守住「文案必须走 `strings.js`」「页头版式不变量」
  「旧品牌名不许回流」这几条约定。它不点界面，所以**不能替代手动加载扩展看一眼**。
- 更新：`cd guilong && git pull`，然后在 `chrome://extensions` 里点扩展卡片上的刷新按钮。
  **改完记得关掉旧的新标签页重新开**——新标签页有缓存，只点刷新按钮不一定生效。
- 本项目是 [Tab Out](https://github.com/zarazhangrui/tab-out) 的衍生版（MIT），原始署名与许可证原文见 `LICENSE`。
