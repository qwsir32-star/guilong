node_modules/

# 可分发的 zip（tools/build-store.js 的产物，别提交）
dist/
*.log
*.db
.DS_Store

# Personal config (landing page patterns, etc.) — never push to GitHub
extension/config.local.js

# 扩展签名私钥 —— 只在打包 .crx 时用得上，绝不要提交
*.pem
*.crx
*.key
