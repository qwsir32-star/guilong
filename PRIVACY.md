# 隐私声明

归拢（Guilong）是一个新标签页扩展。这一页说清楚它存什么、发什么、为什么。

最后更新：2026-09-18

## 一句话

**归拢不收集、不上传、不出售任何用户数据。** 它没有一个自己的服务器，也没有账号。

## 存在你机器上的东西

以下都存在浏览器的 `chrome.storage.local`（Firefox 里是扩展自己的本地存储），**只在本机，不往任何服务器同步**：

| 数据 | 用途 |
|---|---|
| 你钉住的常用站点（网址、名字、图标地址） | 站点条 |
| 你隐藏的常用站点 | 不显示在站点条上 |
| 「稍后再看」的条目与归档 | 未看完的标签页 |
| 显示开关（站点条 / 搜索框 / 天气）、主题色 | 设置 |
| 天气城市与最近一次天气结果 | 缓存，避免每次开新标签页都去拉一次 |

这些数据**归拢自己都读不到**——没有服务端，也就没有地方能让它们离开你的机器。卸载扩展时浏览器会把它们一起清掉。

## 唯一一次对外请求：天气

归拢默认显示你选的城市的气温。这是**整个扩展唯一一次联网**，取的是 [Open-Meteo](https://open-meteo.com/) 的公开预报接口。

**发出去的只有你选的城市坐标两个数字**（默认上海：北纬 31.22222、东经 121.45806）。

**不带**：标签页数据、浏览历史、设备标识、cookie、账号、任何能认出你的东西。

城市搜索是**离线**的 —— 用的是随扩展打包的本地城市库，你敲进搜索框里的字不出你的机器。

不想发这次请求，把设置里的「天气」开关关掉即可，其他功能不受影响。

## 为什么它不需要「读取您在所有网站上的数据」

Open-Meteo 的响应头带着 `access-control-allow-origin: *`，扩展页可以直接按普通 CORS 请求取到它，**一个额外权限都不用加**。这是实测过的。

代价是：这个接口哪天挂了，天气会**静默**地不显示 —— 不会弹错误条，因为新标签页是最不该出现错误提示的地方。

## 权限用途

| 权限 | 干什么 | 不干什么 |
|---|---|---|
| `tabs` | 读出你开着的标签页（网址、标题）来摆这张仪表盘；关标签页；打开标签页 | 不上传、不记录、不统计 |
| `activeTab` | 同上，浏览器在需要时临时授予 | — |
| `storage` | 存上面那张表里的东西 | 不上传 |
| `topSites` | 读浏览器自己的「最常访问」列表，把常用站点条自动补足 | 不上传、不外发；你可以手动钉住、也可以整条关掉 |
| `favicon` | 读**浏览器本地缓存**的站点图标（`_favicon` 端点，Chromium 专有） | 这是本地读盘，不是去站点下载。界面里**所有**图标都走这一条 —— 包括域名卡片里的每个 chip、展开后的 chip、以及「稍后再看」的每一条 |
| `search` | 把你输入的关键字交给**你自己设的默认搜索引擎** | 归拢不指定也不记录是哪家；取不到时退到必应 |

## 三方与追踪

**没有**分析、没有遥测、没有广告、没有第三方 SDK、没有 cookie。

界面字体（DM Sans / Newsreader）**随扩展打包**，不从 Google Fonts 或任何 CDN 联网取 —— 所以开新标签页不会向第三方暴露你的 IP。汉字用你系统自己的字体（苹方 / 微软雅黑）。

## Firefox 版

Firefox 版在 manifest 里显式声明了 `data_collection_permissions: { required: ["none"] }` —— 机器可读的「什么都不收集」。

Firefox 上常用站点的自动图标是**首字母色块**，原因是不存在 Chromium 那个本地 `_favicon` 端点；为了一张图标去访问站点自己的服务器，会把上面「唯一一次联网」这句话戳破，不值得。

## 变更

以后如果加了任何会往外发数据的功能，这一页会先改。

## 联系

问题与反馈走 GitHub Issues：<https://github.com/qwsir32-star/guilong/issues>
