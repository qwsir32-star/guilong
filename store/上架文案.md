# 三家商店的上架文案

照着填就行。三家的字段名字不一样，但内容大部分能复用。

> ⚠️ 各家后台的字段限制会变，提交页面上显示的字数上限以页面为准。
> 下面标的上限是我核过的（2026-09）。

---

## 通用

### 名称

```
归拢 · 新标签页
```

（7 个字符，三家都远在上限内：Chrome 45 / Edge 45 / Firefox 50）

### 一句话简介

```
把散着的标签页归拢到一处：按域名分组、一键关干净、没看完的先存起来。
```

### 详细描述（三家通用版）

```
散着的标签页，归拢一下。

归拢接管你的新标签页，把你此刻开着的所有标签页按域名摆成一张网格仪表盘：
哪个站开了多少个、哪些其实开重了、哪些只是"先放着回头再看"，一眼扫完。

【一眼看尽】
打开的标签页按域名分组，网格排开。

【重复检测】
同一个页面开了两遍会被标出来，可一键清理。

【稍后再看】
舍不得关的先存进清单；处理完的归档，之后还能还原回来。

【常用站点条】
常去的站点钉在顶部，可按住拖动排序，一点就到。

【当地天气】
页头下面一行：图标 + 气温 + 城市 + 今天的最高最低。城市在设置里搜着选，
可以整个关掉。

【主题色】八套可选，含跟随系统。
【中英双语】默认跟随系统，界面文案集中在同一张表里。

【隐私】
没有服务器、不用账号、不需要 API key。标签页数据、钉住的站点、稍后再看、
设置全存在浏览器本地，不往任何服务器发。唯一的对外请求是天气：发的是你选的
城市坐标两个数字，不带标签页数据、浏览历史、设备标识、cookie。
不想发就把天气开关关掉。

【开源】
MIT 许可，源码在 GitHub 上：https://github.com/qwsir32-star/guilong
```

### 分类

| 商店 | 分类 |
|---|---|
| Chrome Web Store | Productivity（工作效率） |
| Edge Add-ons | Productivity |
| Firefox (AMO) | Tabs（标签页） → 次选 Productivity |

### 关键词 / 标签

```
tabs, tab manager, new tab, 标签页, 新标签页, productivity, 标签管理
```

### 隐私政策

```
https://github.com/qwsir32-star/guilong/blob/main/PRIVACY.md
```

### 主页

```
https://github.com/qwsir32-star/guilong
```

---

## Chrome Web Store

- **费用**：开发者账号一次性 5 美元（不是每个扩展）。
- **图标**：128×128 PNG（`extension/icons/icon128.png`，现成的）。
- **截图**：至少 1 张，**1280×800 或 640×400**，最多 5 张。
- **宣传图**（可选但要凑齐才好看）：
  - 小方块 440×280
  - 大方块 920×680
  - 横幅 1400×560
- **数据安全表单**（Data Safety）——照下面填：
  - 是否收集用户数据：**否**
  - 是否用于**与用户无关的**目的（分析/广告）：**否**
  - 数据是否加密传输：不适用（不外发）
  - 单点用途声明（Single purpose）：「Manage and organize open browser tabs」
- **审核**：首次通常 1–3 天。

### 权限说明（CWS 会让你逐条解释用途）——照抄

| 权限 | 说明 |
|---|---|
| `tabs` | 读取当前打开的标签页（网址、标题）以按域名分组展示；关闭与切换标签页。数据仅在本机处理，不外发。 |
| `activeTab` | 同上，浏览器在用户操作时临时授予。 |
| `storage` | 在本地保存用户钉住的站点、「稍后再看」清单、显示开关与主题偏好。 |
| `topSites` | 读取浏览器「最常访问」列表，用于自动补足常用站点条。不外发。 |
| `favicon` | 读取浏览器本地缓存的站点图标（`_favicon` 端点），是本地读盘，不向站点发请求。 |
| `search` | 把用户输入的关键词交给用户自己设置的默认搜索引擎。扩展不指定也不记录是哪家。 |

---

## Edge Add-ons

- **费用**：免费，需要一个 Microsoft 合作伙伴中心账号（要有 Microsoft 账户）。
- **包**：**和 Chrome 用同一个 zip**（`dist/guilong-<版本>-chrome.zip` 或 `-edge.zip`，内容完全一样）。Edge 是 Chromium，不需要任何改动。
- **截图**：至少 1 张，尺寸与 Chrome 通用（1280×800）。
- **审核**：通常比 Chrome 快一些。

Edge 的后台字段比 Chrome 少，名称和描述直接复用上面的通用版即可。

---

## Firefox (AMO)

- **费用**：免费。
- **包**：`dist/guilong-<版本>-firefox.zip`（**不能用 Chrome 那个**）。
- **名称上限 50**，**简介上限 250**（上面的都远在范围内）。
- **截图**：至少 1 张，建议 3–5 张，宽度 ≥1000px 看着最清楚。
- **首页**：填 GitHub 地址。
- **隐私政策**：填 `PRIVACY.md` 的地址。

### AMO 的三条硬要求（打包脚本已经做好了，这里记录一下为什么）

1. **`data_collection_permissions`** —— 2025-11 起所有新扩展必填。归拢写的是
   `{ "required": ["none"] }`，意思是什么都不收集。
2. **`browser_specific_settings.gecko.id`** —— MV3 签名必填，AMO 不再自动分配。
   归拢用的是 `guilong@qwsir32-star.github.io`。**首次提交就定死，之后别改。**
3. **`strict_min_version: "142.0"`** —— 版本地板是算术：根证书过期要 ≥115 ESR/128，
   后台双键要 ≥121，`data_collection_permissions` 要桌面 ≥140 / Android ≥142。
   取 142 三个都过，lint 一条警告都没有。

### 提交时要勾的

- **兼容平台：只勾 Desktop。** Firefox for Android **不支持** `chrome_url_overrides`
  （抢新标签页在 Android 版上根本不生效），勾上去就是个坏的核心功能。
- **源码提交**：归拢是零构建的 —— 上传的就是你写的那份源码，没有压缩也没有打包器，
  所以**不需要**走「提交构建产物 + 源码」那道关。真被问到，给 GitHub 仓库地址即可。

### 提交前跑一次

```bash
npx web-ext lint dist/firefox
```

目标：**0 errors / 0 warnings / 0 notices**。

### 审核备注（Reviewer notes）里建议主动说明

> 这个扩展会联网一次：向 Open-Meteo 的公开预报接口发送用户选择的城市坐标
> （两个数字），用于显示气温。城市搜索是离线的（用随扩展打包的本地城市库）。
> 除此之外没有任何外发 —— 界面字体（DM Sans / Newsreader）也是随包打包的，
> 不从 Google Fonts 或任何 CDN 取。可以在设置里关掉天气。

主动说比被问到再说快得多 —— 审核员看到「新标签页扩展为什么要联网」一定会问。

---

## 截图：准备什么

三家都要。等真机截图拿到后，按这个清单准备：

| # | 画面 | 怎么造出这个画面 | 说明 |
|---|---|---|---|
| 1 | 默认主题（纸感）的仪表盘 | 开 8~12 个标签页，跨 5~7 个域名 | 主图，要能看到按域名分组的网格 |
| 2 | 深色主题 | 设置 → 主题 → 深色 | 展示主题色 |
| 3 | 设置面板展开 | 点页头右上角那个齿轮 | 展示开关、主题选择、天气城市 |
| 4 | 稍后再看 / 归档区 | 在几个 chip 上点书签图标，或「全部存起来」 | 展示第二块功能 |
| 5 | ~~关掉一批标签页时的彩带~~ | — | **不做**（2026-09-19 定）。静态图很难卡准，录屏又要为三家商店各做一份，不值。 |

尺寸：1280×800（Chrome / Edge 通用），Firefox 用同一批即可。
AMO 还建议再裁一张小图做预览图。

⚠️ **有一张必须专门造，不然截图里看不见**：从**同一个域名**开 10 个以上标签页，
才会出现「还有 N 个」那个折叠 chip。顺手在同一域名下开两个完全相同的网址，
还能带出这张卡的**橙色状态条 + 琥珀色「N 个重复」徽章 + 重复那条 chip 上的 `(2x)`
+ 「关闭 N 个重复」按钮**。

⚠️ **别把它和 `#tabOutDupeBanner` 搞混**：那个横幅说的是「你现在开着 N 个**归拢**
标签页，只留当前这一个？」，是 `app.js:1613` 按 `t.isTabOut`（URL 等于扩展自己的
新标签页）过滤出来的，跟「同一网址开两次」没有关系。商店截图里**不该出现**它。

**拍之前先看一眼这三处 —— 它们会把私人信息带进公开页面：**

- **天气条**会显示你设的城市（例如「上海 25°」）。不想公开就先进设置换个城市。
- **常用站点条**（如果打开了）读的是浏览器真实的「最常访问」，等于公开你的浏览习惯。
  首装时它默认是关的，但要拍它就得先打开。
- **标签页标题和网址**会原样落在画面里。**别用平时那堆标签页**，
  专门开一组「演示用」的最省事。

**怎么交给我**：不用纠结尺寸 —— 按方便的方式截就行（Retina 下是 2560×1600 也没关系），
但**只截浏览器窗口，不要截整个桌面**（桌面上别的东西也会一起进画面）。
丢进 `outputs/guilong-screenshots/`（原图放 `raw/` 子目录，成品按
`01-dashboard.png` 这类名字平铺在该目录下）。
裁成 1280×800、去窗口边框这些我来做，做完再放进 README 和三家商店。

⚠️ **只截窗口还不够 —— 裁图时必须把标签栏和书签栏整条切掉**：
它们会带出拍摄者真实的书签和标签页标题，等于把私人浏览习惯一起公开出去。

### 那两个画面有现成的页面，不用自己开标签页

「折叠 chip」和「重复」已经做成现成的一页（`tools/make-screenshot-page.js`）：
把演示数据喂给**真的 `app.js`** 渲染，不用手动凑十几个标签页、也不用担心漏状态。

```bash
node tools/make-screenshot-page.js     # 生成两页（字体内联，单文件自包含）
node tools/check-screenshot-page.js    # 真 Chrome 验一遍（⚠️ 要脱离沙箱跑）
```

| 文件 | 内容 |
|---|---|
| `outputs/guilong-全新安装预览.html` | 全默认：搜索框关、常用站点条关、稍后再看空 |
| `outputs/guilong-折叠与重复预览.html` | github 一家 13 条 →「还有 4 个」折叠 chip + 橙条 +「1 个重复」 |

两页都一屏拍得下（1280×800 里仪表盘底分别落在 **790px / 778px**）。
⚠️ 改这两页的演示数据时，`tools/check-screenshot-page.js` 里那 19 条专属断言
会跟着红 —— 那正是它拦「页面还在、但什么也演示不出来」的地方。
